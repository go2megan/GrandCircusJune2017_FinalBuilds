using System;
namespace MJNorton_DateDiff
{
    public class ComputeDates
    {
        static void Main(string[] args)

        {
            int dateA, dateB, result; //Named variables to pass for dates

            //Asking user for dates to compare
            Console.WriteLine("Enter a date formatted in dd/mm/yyyy: ");
            dateA = Convert.ToDateTime(Console.ReadLine());//Converting string to date

        }
    }
}
