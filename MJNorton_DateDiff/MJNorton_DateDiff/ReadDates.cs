using System;
namespace MJNorton_DateDiff
{
	public class ReadDates
	{
		static void Main(string[] args)

		{
            //Named variables to pass for dates
            DateTime dateA;
            DateTime dateB;

			//Asking user for dates to compare

			Console.WriteLine("Enter a date formatted in dd/mm/yyyy: ");
            dateA.ToShortDateString(Console.ReadLine());

			//Converting string A to a date

			Console.WriteLine("Enter a date formatted in dd/mm/yyyy: ");
            dateB.ToShortDateString(Console.ReadLine());

            //Converting string B to a date

		}
	}
}
